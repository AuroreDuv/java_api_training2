{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "properties": {
        "consequence": {
            "type": "string",
            "enum": ["miss", "hit", "sunk"]
        },
        "shipLeft": {
            "type": "boolean"
        }
    },
    "required": [
        "consequence",
        "shipLeft"
    ]
}
